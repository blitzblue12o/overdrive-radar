/**
 * Mapbox circle layer ids for unclustered / selected event points.
 * Markers are rendered via GeoJSON layers in EventMap — not React DOM nodes.
 */
export const EVENT_POINT_LAYER_ID = "events-unclustered";
export const EVENT_SELECTED_LAYER_ID = "events-selected";
