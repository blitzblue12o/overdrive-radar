# Required to boot the app (anon key only — never commit service_role)
# Local: http://127.0.0.1:54331  |  Production: https://<project-ref>.supabase.co
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
NEXT_PUBLIC_MAPBOX_TOKEN=

# Server-only — Wave 3 ingestion (never expose to the browser)
SUPABASE_SERVICE_ROLE_KEY=
CRON_SECRET=
